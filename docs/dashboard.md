Dashboard API
   ├── /dashboard/summary
   │      ├─ totalRecognized  → DB
   │      ├─ unknownFaces    → DB
   │      ├─ currentEntries  → presenceMap
   │      └─ totalExits      → DB
   │
   └── /dashboard/events
          → DB (paginated)
